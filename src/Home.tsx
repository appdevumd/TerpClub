import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

export const Home: React.FC = ({}) => {
    return <View>
        <Text style={{fontSize: 100, color: "red"}}>The future is now!</Text>
    </View>
}